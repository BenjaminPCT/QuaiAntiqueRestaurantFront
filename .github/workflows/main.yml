name: 🚀 Deploy website on push

on:
  push:
    branches:
      - main  # Déclenche l'action seulement sur la branche 'main'

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Install dependencies
      uses: actions/setup-node@v2
      with:
        node-version: '14'  

    - name: 📂 Install npm dependencies
      run: npm install

      
    - name: 📜 Run ESLint
      run: npm run lint
      # Assurez-vous que vous avez un script lint configuré dans package.json
      # Exemple: "lint": "eslint ."

    - name: 🧪 Run Tests
      run: npm test
      # Assurez-vous que votre script test est configuré dans package.json
      # Exemple: "test": "jest"

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp-quai-antique-votrerestaurant.alwaysdata.net
        username: quai-antique-votrerestaurant	
        password:  ${{ secrets.ftp_password }}
        protocol: ftps     # Spécifiez FTPS pour utiliser une connexion sécurisée
        secure: true       # Activer TLS/SSL pour la connexion
        passive: true      # Mode passif souvent recommandé
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/bootstrap/scss/**
          **/node_modules/bootstrap/js/**
          **/node_modules/bootstrap-icons/icons/**  
        server-dir: /www/

    
