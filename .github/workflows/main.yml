name: 🚀 Deploy website on push

on:
  push:
    branches:
      - main  # Déclenche l'action seulement sur les pushs vers la branche 'main'

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: mgrenier/ftp-deploy-action@v1.0.0
      with:
        ftp-server: ftp://ftp-quai-antique-votrerestaurant.alwaysdata.net
        ftp-username: quai-antique-votrerestaurant
        ftp-password: ${{ secrets.ftp_password }}
        local-dir: .  # Répertoire local à synchroniser
        server-dir: /www/
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/bootstrap/scss/**
          **/node_modules/bootstrap/js/**
          **/node_modules/bootstrap-icons/icons/**

