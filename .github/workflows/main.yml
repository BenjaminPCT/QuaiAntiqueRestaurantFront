name: 🚀 Deploy website on push

on:
  push:
    branches:
      - main  # Déclenche l'action seulement sur les pushs vers la branche 'main'

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp-quai-antique-votrerestaurant.alwaysdata.net
        username: quai-antique-votrerestaurant
        password: ${{ secrets.ftp_password }}
        protocol: ftp  # Assurez-vous que c'est bien ftp pour une connexion non sécurisée
        passive: true  # Le mode passif est souvent recommandé pour éviter des problèmes de connexion
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/bootstrap/scss/**
          **/node_modules/bootstrap/js/**
          **/node_modules/bootstrap-icons/icons/**  
        server-dir: /www/

